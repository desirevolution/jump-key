services:
  jump-key-dev:
    user: root
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      #      - .:/app
      #      - /app/node_modules
      - ./config:/app/config
      - ./icons:/app/icons
    ports:
      - "8080:8080"
